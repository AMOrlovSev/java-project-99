spring:
  config:
    activate:
      on-profile: "production"
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME:for_all_projects_user}
    password: ${DB_PASSWORD:RaDpGx71O4oq3i5N0c5xNnJQECran4GE}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false